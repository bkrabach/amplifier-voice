# Home Assistant Integration Configuration
# Copy this file to .env and update with your actual values

# HOST: The hostname or IP address of your Home Assistant instance
# Examples: "homeassistant.local", "192.168.1.100", "localhost"
HOST=your-home-assistant-host

# PORT: The port number Home Assistant is running on
# Default is typically 8123 for HTTP or 443 for HTTPS
PORT=8123

# ACCESS_TOKEN: Long-lived access token for authenticating with Home Assistant
# Generate one in Home Assistant: Profile -> Security -> Long-Lived Access Tokens
# Keep this token secret and never commit it to version control
ACCESS_TOKEN=your-long-lived-access-token
